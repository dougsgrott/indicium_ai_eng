# ==============================================================================
# INDICIUM AI ENGINEERING - ENVIRONMENT CONFIGURATION
# ==============================================================================
# This is an example environment file. Copy this to .env and fill in your values.
#
# IMPORTANT: Never commit the actual .env file to version control!
# ==============================================================================

# === API Keys (REQUIRED) ===

# OpenAI API Key - REQUIRED for LLM functionality
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Tavily API Key - OPTIONAL but recommended for news search
# Get yours at: https://tavily.com/
TAVILY_API_KEY=tvly-your-tavily-api-key-here

# === LangSmith Tracing (OPTIONAL) ===

# Enable/disable LangSmith tracing
# Set to "true" to enable, "false" to disable
LANGCHAIN_TRACING_V2=false

# LangChain/LangSmith API Key
# Get yours at: https://smith.langchain.com/
LANGCHAIN_API_KEY=lsv2_pt_your-langchain-api-key-here

# LangSmith project name (customize as needed)
LANGCHAIN_PROJECT=formacao_ai_indicium

# === Langfuse Observability (OPTIONAL - Self-Hosted) ===

# Enable/disable Langfuse tracing
# Set to true to enable (requires Docker services running)
LANGFUSE_ENABLED=false

# Langfuse API Keys (auto-generated by setup.sh or get from Langfuse UI)
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key-here
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key-here

# Langfuse host (default for local Docker setup)
LANGFUSE_HOST=http://localhost:3000

# ==============================================================================
# NOTES
# ==============================================================================
#
# 1. Required vs Optional:
#    - OPENAI_API_KEY: REQUIRED
#    - TAVILY_API_KEY: Optional (news search won't work without it)
#    - LangSmith keys: Optional (for debugging/tracing)
#    - Langfuse keys: Optional (for self-hosted observability)
#
# 2. Getting API Keys:
#    - OpenAI: https://platform.openai.com/ (paid service)
#    - Tavily: https://tavily.com/ (free tier available)
#    - LangSmith: https://smith.langchain.com/ (free tier available)
#
# 3. Security:
#    - Never share your API keys
#    - Never commit .env to git (it's in .gitignore)
#    - Rotate keys if accidentally exposed
#
# ==============================================================================
